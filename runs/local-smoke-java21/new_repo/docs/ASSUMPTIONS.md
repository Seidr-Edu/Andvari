# Assumptions

- The source diagram is `docs/diagram.puml` and has title "Andvari Reconstruction Example".
- The mock reconstruction demonstrates a deterministic parser summary component instead of a full domain reconstruction.
- Java 21 is targeted via Maven compiler release configuration.
